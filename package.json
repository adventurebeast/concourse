{
  "name": "concourse",
  "version": "0.0.3",
  "description": "A stupidly simple IDE built for driving AI coding agents.",
  "main": "./out/main/index.js",
  "type": "module",
  "author": "",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "bump": "npm version patch --no-git-tag-version",
    "clean": "rm -rf out release",
    "dist": "npm run bump && npm run clean && electron-vite build && electron-builder --mac",
    "pack": "npm run bump && npm run clean && electron-vite build && electron-builder --mac --dir",
    "postinstall": "electron-rebuild -f -w node-pty"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "simple-git": "^3.36.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.1",
    "electron": "^33.0.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^2.3.0",
    "monaco-editor": "^0.52.0",
    "vite": "^5.4.0"
  }
}
